let frutas = ["maçã", "banana", "laranja", "uva"];

for (let i = 0; i < frutas.length; i++) {
  console.log("Fruta:", frutas[i]);
}

// Neste exemplo, declaramos um array chamado frutas que contém várias frutas como elementos. Em seguida, utilizamos um loop for para percorrer cada elemento do array e exibi-lo no console.

let abc = ['a','b','c'];
for (let i = 0; i < abc.length; i++) {
console.log("abc:",abc[i])
if (abc[i] == 'b'){
console.log("Condicional b!")
}
}