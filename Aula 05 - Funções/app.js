function saudacao(nome) {
    console.log("Olá, " + nome + "!");
  }
  
  saudacao("João");

  
// Neste exemplo, declaramos uma função chamada saudacao que recebe um parâmetro nome. Dentro da função, exibimos uma mensagem de saudação personalizada no console, concatenando o valor do parâmetro nome. Em seguida, chamamos a função saudacao e passamos o valor "João" como argumento.

// Agora, é a sua vez de criar uma função e chamá-la com argumentos personalizados.